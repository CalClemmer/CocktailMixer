<style>
#ingredient2 {
    color: red;
}
</style>

<h2>Homepage</h2>
<h3> Ingredients </h3>
<input type="text" id="myInput" onkeyup="searchBar()" placeholder="Search for names..">

<ul id="myUL">
    <% ingredients.forEach(function(ingredient) { %>
    <li><a id='ingredient<%= ingredient.id%>' href="#"> <%= ingredient.name %> </a></li>
    <% }); %>
</ul>
My Ingredients
<ul>
    <% inventory.forEach(function(ingredient) { %>
    <li><a href="#"> <%= ingredient.name %> </a></li>
    <% }); %>
</ul>

<script>

function searchBar() {
  // Declare variables
  var input, filter, ul, li, a, i, txtValue;
  input = document.getElementById('myInput');
  filter = input.value.toUpperCase();
  ul = document.getElementById("myUL");
  li = ul.getElementsByTagName('li');

  // Loop through all list items, and hide those who don't match the search query
  for (i = 0; i < li.length; i++) {
    a = li[i].getElementsByTagName("a")[0];
    txtValue = a.textContent || a.innerText;
    if (txtValue.toUpperCase().indexOf(filter) > -1) {
      li[i].style.display = "";
    } else {
      li[i].style.display = "none";
    }
  }
}

async function loadInventory(){
    try {
        //const allIngredients = await Ingredients.findAll({});

        const parsedIngredients = await Ingredients.findAll({});
        console.log(parsedIngredients);

        res.render('index', { ingredients: parsedIngredients });

    } catch (err) {
        console.log(err);
    }
}

async function addIngredient(name, species, description) {

try {
const user1 = await User.findOne({
    where: {id: 1},
    include: [Pet]

})
console.log(user1.toJSON());

const newPet = await user1.createPet({name, species, description})
console.log(newPet.toJSON());

} catch (error) {
    console.log(error);
}
}

</script>